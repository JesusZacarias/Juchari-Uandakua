import 'package:flutter/material.dart';

class CustomNavigatorItem extends StatelessWidget {
  const CustomNavigatorItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

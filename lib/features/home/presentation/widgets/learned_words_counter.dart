import 'package:flutter/material.dart';

class LearnedWordsCounter extends StatelessWidget {
  const LearnedWordsCounter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
